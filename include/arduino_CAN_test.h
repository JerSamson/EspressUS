#include <CAN.h>

class arduino_CAN_test
{
private:
    /* data */
public:
    bool init_CAN(uint8_t ctx, uint8_t crx);
    void test_CAN();
    // arduino_CAN_test(/* args */);
    // ~arduino_CAN_test();
};